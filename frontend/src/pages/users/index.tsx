export default function UserIndex() {
  return (
    <>
      <h2>ユーザー一覧</h2>

      {/* <ul>
        {users.map((user, index) => {
          return (
            <li key={index}>
              <p>user</p>
            </li>
          );
        })}
      </ul> */}
    </>
  );
}
